package com.ragnar.primordialauthority.registry;

public final class TemplateConfigs {
    private TemplateConfigs() {
    }

    /*
     * When you are ready to add your first config:
     *
     * import com.ragnar.primordialauthority.config.ExampleCommonConfig;
     * import io.github.manasmods.manascore.config.ConfigRegistry;
     *
     * public static void init() {
     *     ConfigRegistry.registerConfig(new ExampleCommonConfig());
     * }
     */
    public static void init() {
        // Register ManasCore config classes here after you create them.
    }
}
