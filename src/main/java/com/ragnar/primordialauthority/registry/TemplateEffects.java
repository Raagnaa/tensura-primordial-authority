package com.ragnar.primordialauthority.registry;

import net.neoforged.bus.api.IEventBus;

public final class TemplateEffects {
    private TemplateEffects() {
    }

    /*
     * When you are ready to add your first effect:
     *
     * 1. Copy effect/ExampleBasicEffect.java.example to ExampleBasicEffect.java.
     * 2. Add the NeoForge DeferredRegister or the registry helper used by your setup.
     * 3. Register it from this class, then add its lang key.
     */
    public static void init(IEventBus modEventBus) {
        // Add effect registrations here after you create your first effect.
    }
}
